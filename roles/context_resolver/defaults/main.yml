---
required_host_metadata:
  - application_id
  - application_family
  - environment_type
  - site
  - resource_class

# auto: rollout si target_ring est fourni, sinon standalone
context_resolver_mode: auto
